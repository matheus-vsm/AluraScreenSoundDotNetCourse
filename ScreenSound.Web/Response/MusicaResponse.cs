namespace ScreenSound.Web.Response
{
    public record MusicaResponse(int Id, string Nome, int ArtistaId, string NomeArtista); // Define a record type for MusicaResponse with properties Id, Nome, ArtistaId, and NomeArtista
}
